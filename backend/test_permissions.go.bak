package main

import (
	"fmt"
	"app-sistem-akuntansi/models"
)

func main() {
	fmt.Println("🔍 Testing GetDefaultPermissions for admin role...")
	
	permissions := models.GetDefaultPermissions("admin")
	
	fmt.Printf("📊 Found %d modules with permissions:\n", len(permissions))
	
	for module, perm := range permissions {
		fmt.Printf("🔧 Module: %s\n", module)
		fmt.Printf("   CanView: %v\n", perm.CanView)
		fmt.Printf("   CanCreate: %v\n", perm.CanCreate)
		fmt.Printf("   CanEdit: %v\n", perm.CanEdit)
		fmt.Printf("   CanDelete: %v\n", perm.CanDelete)
		fmt.Printf("   CanApprove: %v\n", perm.CanApprove)
		fmt.Printf("   CanExport: %v\n", perm.CanExport)
		fmt.Println()
	}
	
	// Specifically check reports module
	if reportsPerm, exists := permissions["reports"]; exists {
		fmt.Printf("✅ Reports module permissions found:\n")
		fmt.Printf("   CanView: %v\n", reportsPerm.CanView)
		fmt.Printf("   CanCreate: %v\n", reportsPerm.CanCreate)
		fmt.Printf("   CanEdit: %v\n", reportsPerm.CanEdit)
		fmt.Printf("   CanDelete: %v\n", reportsPerm.CanDelete)
		fmt.Printf("   CanApprove: %v\n", reportsPerm.CanApprove)
		fmt.Printf("   CanExport: %v\n", reportsPerm.CanExport)
	} else {
		fmt.Println("❌ Reports module not found in default permissions!")
	}
}